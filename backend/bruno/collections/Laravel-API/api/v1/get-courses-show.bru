meta {
  name: Courses Show
  type: http
  seq: 10
}
get {
  url: {{baseUrl}}/api/v1/admin/courses/{{course}}
  body: none
  auth: bearer
}
headers {
  Accept: application/json
}
auth:bearer {
  token: {{authToken}}
}
