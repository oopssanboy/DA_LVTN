meta {
  name: Subjects Show
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/api/v1/admin/subjects/{{subject}}
  body: json
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{authToken}}
}

vars:pre-request {
  subject: 
}
