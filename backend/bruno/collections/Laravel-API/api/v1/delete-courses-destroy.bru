meta {
  name: Courses Destroy
  type: http
  seq: 13
}
delete {
  url: {{baseUrl}}/api/v1/admin/courses/{{course}}
  body: none
  auth: bearer
}
headers {
  Accept: application/json
}
auth:bearer {
  token: {{authToken}}
}
