meta {
  name: Attempt Detail
  type: http
  seq: 11
}
get {
  url: {{baseUrl}}/api/exams/{{exam}}/attempts/{{attempt}}
  body: none
  auth: bearer
}
headers {
  Accept: application/json
}
auth:bearer {
  token: {{authToken}}
}
