meta {
  name: Logout
  type: http
  seq: 16
}

post {
  url: {{baseUrl}}/api/logout
  body: json
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{authToken}}
}
